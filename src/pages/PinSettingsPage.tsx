import React from "react";
import PinSettings from "@/features/pengaturan/components/PinSettings";

const PinSettingsPage: React.FC = () => {
  return (
    <div className="min-h-screen bg-surface p-6">
      <PinSettings />
    </div>
  );
};

export default PinSettingsPage;
